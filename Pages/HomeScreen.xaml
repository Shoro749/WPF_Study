<UserControl x:Class="WPF_Study.Pages.HomeScreen"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:WPF_Study.Pages"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <TextBox
    Height="40" x:Name="tb_Login" Grid.Row="1" Width="150"
    VerticalAlignment="Center" HorizontalAlignment="Center"
    materialDesign:HintAssist.Hint="Enter Name"
    AcceptsReturn="True"
    VerticalScrollBarVisibility="Auto" />
        <Button Content="Enter" x:Name="b_Enter" Grid.Row="1" VerticalAlignment="Bottom" Width="150" Click="Button_Click"/>
    </Grid>
</UserControl>
